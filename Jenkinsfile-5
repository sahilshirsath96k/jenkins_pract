pipeline {
  agent any 
  parameters {
    choice(name: "ENVIRONMENT", choices: ['staging', 'production'], desciption: 'Target')
  }
  stages {
    stage('Deploy') {
      steps {
        sh "echo Deploying to ${ params.ENVIRONMENT }"
      }
    }
  }
}
